version: '3.8'

services:
  ai-service:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - ./storage:/app/storage
    environment:
      - APP_ENV=production
      - DEMUCS_MODEL=htdemucs
      - WHISPER_MODEL=base
    restart: unless-stopped